# Copy to frontend/.env (Vite reads this folder). Never commit .env.

# Django API base for dev server proxy (no trailing slash).
# Local backend:
VITE_DJANGO_URL=http://127.0.0.1:8000
# Railway backend (jab tum URL doge yahan paste karo), e.g.:
# VITE_DJANGO_URL=https://your-service.up.railway.app
