# Copy to frontend/.env (Vite reads this folder). Never commit .env.

# Django API base — dev proxy + baked into build for fetch() (no trailing slash).
# Default in vite.config.js is Railway backend; override for local Django:
# VITE_DJANGO_URL=http://127.0.0.1:8000
VITE_DJANGO_URL=https://web-production-9e2ed.up.railway.app
